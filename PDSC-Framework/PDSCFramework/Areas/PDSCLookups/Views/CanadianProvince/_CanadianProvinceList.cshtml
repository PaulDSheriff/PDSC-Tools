@using PDSC.Common.EntityLayer
@model PDSC.Common.ViewModelLayer.CanadianProvinceViewModel

@if (Model.DataCollection == null || Model.DataCollection.Count == 0) {
  <p>Enter search parameters, then click Search button</p>
}
else {
  <form method="get" asp-action="SortPage">
    <partial name="_SortPageHiddenFields" />

    <table class="table table-bordered table-hover table-striped">
      <thead>
        <tr>
          <th>Actions</th>
          <th>
            <a href="#" onclick="pdscController.sortOnHeaderClick('ProvinceCode');">
              Province Code
            </a>
          </th>
          <th>
            <a href="#" onclick="pdscController.sortOnHeaderClick('ProvinceName');">
              Province Name
            </a>
          </th>
          <th>Delete</th>
        </tr>
      </thead>
      <tbody>
        @foreach (CanadianProvince item in Model.DataCollection) {
        <tr>
          <td>
            <a asp-action="Edit"
               asp-route-id="@item.ProvinceCode"
               class="btn btn-primary">
              Edit
            </a>
          </td>
          <td>@Html.DisplayFor(m => item.ProvinceCode)</td>
          <td>@Html.DisplayFor(m => item.ProvinceName)</td>
          <td>
            @Html.ActionLink("Delete", "Delete",
                new { id = item.ProvinceCode },
                new
                  {
                    onclick = "return confirm('Delete this Canadian Province?');",
                    @class = "btn btn-danger"
                  })
          </td>
        </tr>
        }
      </tbody>
    </table>
    <partial name="_PagerPartial" />
  </form>
}
